import { Component } from '@angular/core';

@Component({
    selector: 'app-regex-tester',
    imports: [],
    templateUrl: './regex-tester.spec.html',
    styleUrl: './regex-tester.spec.scss'
})
export class RegexTesterSpec {}
